<%= form_with(model: search_datum) do |form| %>
  <% if search_datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(search_datum.errors.count, "error") %> prohibited this search_datum from being saved:</h2>

      <ul>
        <% search_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :article_id, style: "display: block" %>
    <%= form.text_field :article_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
